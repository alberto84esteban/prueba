import { Ref } from 'vue';
import { VaDatePickerModelValue } from '../../va-date-picker/types/types';
export declare const useDateParser: (props: {
    parse?: ((input: string, isValidRef?: Ref<boolean> | undefined) => VaDatePickerModelValue) | undefined;
    parseDate?: ((input: string) => Date) | undefined;
    delimiter: string;
    rangeDelimiter: string;
}) => {
    parseDateInputValue: (text: string) => VaDatePickerModelValue;
    isValid: Ref<boolean>;
};
